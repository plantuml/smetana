@TranslatedBy("plantuml")
public static StarVoid memalloc(StarVoid heap, size_t request) {
    StarVoid rv = malloc(request);
    Memory.memset(rv, 0, request);
    return rv;
}
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
static void *memalloc(void *heap, size_t request)
{
    void *rv;
    (void) heap;
    rv = malloc(request);
    memset(rv, 0, request);
    return rv;
}

