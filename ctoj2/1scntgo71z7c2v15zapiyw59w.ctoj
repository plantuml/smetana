@TranslatedBy("plantuml")
public static StarChar refstrbind(Dict_t strdict, StarChar s) {
    refstr_t r;
    r = refsymbind(strdict, s);
    if (r!=null)
	return r.s;
    else
	return null;
}

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
static char *refstrbind(Dict_t * strdict, char *s)
{
    refstr_t *r;
    r = refsymbind(strdict, s);
    if (r)
	return r->s;
    else
	return ((char *)0);
}

