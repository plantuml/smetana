public static double B01(double t) {
    double tmp = 1.0 - t;
    return tmp * tmp * (tmp + 3 * t);
}

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
static double B01(double t)
{
    double tmp = 1.0 - t;
    return tmp * tmp * (tmp + 3 * t);
}

